{
  "name": "durable-object",
  "version": "0.0.49",
  "private": true,
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "cojson-storage-do-sqlite": "workspace:*",
    "jazz-tools": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "typescript": "catalog:default",
    "vitest": "~3.2.4",
    "wrangler": "^4.42.0"
  }
}
